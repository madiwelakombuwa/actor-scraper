# Cloudflare Pages configuration for Facebook Dashboard
name = "facebook-dashboard"
compatibility_date = "2024-01-01"

# Pages configuration - build settings must be configured in Cloudflare Dashboard
pages_build_output_dir = "public"
